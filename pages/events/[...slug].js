//  slug route
//  it works when we enter more than one dynamic parameter after /events

export default function FilteredEventsPage(params) {
  return (
    <div>
      <h1>Filtered Events</h1>
    </div>
  );
}
